extends ProperResource
class_name Enemy

@export var name: String
@export var hit_points: int = 70
var max_hit_points := hit_points
var shield_amount: int
# @export var attack_pattern: Array[Attacks]
# var next attack index
